# If not running interactively, don't do anything (leave this at the top of this file)
[[ $- != *i* ]] && return

DOTFILES_BASE_DEFAULTS=0

for rc in \
  "$HOME/.local/share/omarchy/default/bash/rc" \
  "$HOME/.local/share/omadots/default/bash/rc" \
  "$HOME/.local/share/omadots/bash/rc" \
  "$HOME/.omadots/default/bash/rc" \
  "$HOME/.omadots/bash/rc"; do
  if [ -r "$rc" ]; then
    source "$rc"
    DOTFILES_BASE_DEFAULTS=1
    break
  fi
done

[ -r ~/.config/bash/personal.sh ] && source ~/.config/bash/personal.sh
